<h1>Your Shopping Cart</h1>

<% if @cart.order_items.any? %>
  <table>
    <thead>
      <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Price</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @cart.order_items.each do |item| %>
        <tr>
          <td><%= item.product.name %></td>
          <td>
            <%= form_with model: item, url: update_item_cart_path(item), method: :patch, local: true do |f| %>
              <%= f.number_field :quantity, value: item.quantity %>
              <%= f.submit 'Update' %>
            <% end %>
          </td>
          <td><%= item.product.price %></td>
          <td>
            <%= link_to 'Remove', remove_item_cart_path(item), method: :delete %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <h2>Total: <%= @cart.order_items.sum { |item| item.product.price * item.quantity } %></h2>
<% else %>
  <p>Your cart is empty.</p>
<% end %>

<%= link_to 'Continue Shopping', root_path %>
